# @package _global_
# Rolling Composer saves at 300, 600, 900, 1200, 1500 only (5 files total).
# Requires save_num_checkpoints_to_keep >= 5 so early milestones are not deleted.
save_each_n_epochs: 300
save_num_checkpoints_to_keep: 5
checkpoint_milestones: []
epochs: 1500
